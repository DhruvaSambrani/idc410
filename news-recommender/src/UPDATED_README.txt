# Running the script

1. Initialize and activate a `conda` environment. 
2. Run `conda install scikit-learn numpy flask nltk`
3. Run `pip install newspaper3k`
4. If necessary, run `./bootstrap_db.py` and `./bootstrap_users.py` 
5. Run the `./bootstrap_articles.py` script
6. Run `flask run` to start.

